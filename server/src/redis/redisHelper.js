// import {redisClient} from './redisClient.js';

// This function is used to mark all messages as seen when user opens the conversation. It will update the status of all messages in the conversation to SEEN.
// export const markAllMessageSeen = async (senderId, receiverId) => {
//     const chatKey = [senderId, receiverId].sort().join(':');
//     try {
//         await redisClient.set()
//     }
// }